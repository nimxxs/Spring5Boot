<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="nimxxs.hello.boot.spring5boot.mybatis.BoardMapper">
    <insert id="insertBoard" parameterType="Board">
        insert into board2 (userid, title, contents, ipaddr)
        values (#{userid},#{title},#{contents},#{ipaddr});
    </insert>

    <select id="selectBoard" resultType="Board">
        select bno, title, userid, regdate, thumbs, views from board2
        order by bno desc limit #{stnum}, 25
    </select>

    <select id="selectOneBoard" resultType="Board">
        select title, userid, regdate, thumbs, views from board2 where userid = #{userid}
    </select>

</mapper>